#include <stdio.h>
#include <stdlib.h>

#define LENGTH 30

struct Node{
    char *value;
};

struct HashTable{
    struct Node *hashArray[LENGTH];
    int size = LENGTH;
};

int hashIndex(char *key) {


}

st
